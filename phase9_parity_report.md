# Phase 9 parity report

Generated by `tests/test_phase9_parity_audit.py::test_criterion_2_behavioral_parity` on every audit run. Source corpus: `tests/smoke_corpus.jsonl` (28 entries across 5 shapes).

## Cutover gate

**`UNEXPECTED_DIVERGENCE` count: 0**

The cutover gate is `unexpected_divergence == 0`. Other buckets are informational.

## Bucket counts

| Bucket | Count |
| --- | --- |
| `v2_only_by_design` | 8 |
| `expected_divergence` | 8 |
| `unexpected_divergence` | 0 |
| `both_pass` | 11 |
| `both_fail` | 1 |

## Audit scoping

Per the 9a planning conversation: the v2 stack has no `/v2/api/chat` endpoint (Phase 8.5 explicitly out-of-scoped chat streaming). The audit consumes v2 in-process via Layer 1 → Layer 2 → walker → Layer 5 imports — no HTTP — and re-scopes the latency criterion to per-claim units. This is a faithful measure of the post-extraction pipeline; turn-level latency awaits the v0.15 chat endpoint.

The audit covers Layer 1 → Layer 2 → walker → Layer 5 (the post-extraction pipeline). It does not exercise the chat model call, SSE streaming, or session-state propagation across multiple turns; those paths are v0.15 work as documented in the deferred-work section.

Five buckets, not four: `v2_only_by_design` is separated from `expected_divergence` because substrate / two-text / routing-memo entries exercise architectural improvements with no v1 baseline to diverge from.

## Known informational outcomes

**`p7-cheetahs-deriv-assertion` lands in `both_fail` by audit design, not v2 regression.** The smoke corpus runs with shared per-stack state in entry order; by the time this Phase-7 entry runs, `p3-cheetahs-storage` has already stored `dislikes(user, cheetahs, polarity=1)`. v2's walker resolves the model's `likes(user, cheetahs, polarity=0)` claim against that direct fact via predicate_equivalence (likes/dislikes contradictory + polarity flip), reaching `served_from_tier='u'` instead of the corpus's expected `'derivation'`. The corpus author flagged the conflict in `p7-cheetahs-deriv-storage`'s notes — production conversations don't accumulate both facts. v2's behavior here is correct under principle 7's tier-precedence discipline: cheaper resolutions are preferred when available, and direct lookup precedes derivation by design. The audit's shared-state model surfaces this property; per-entry isolation would mask it. We leave the entry as-is and note it here as architecturally-correct behavior, not a regression.

## Per-entry outcomes (corpus order)

### `p1-merco-clean` — user_storage [mereological_pattern] → **expected_divergence**

  * v1 → `fail`: v1 registry has no pattern 'mereological'
  * v2 → `pass`: stored 1 fact(s): mereological.part_of(session_ids=[], affirmed=1)

### `p1-loc-clean` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): spatial_temporal.located_in/unverified
  * v2 → `pass`: stored 1 fact(s): spatial_temporal.located_in(session_ids=[], affirmed=1)

### `p1-disambig-pair` — user_storage [mereological_pattern] → **expected_divergence**

  * v1 → `fail`: v1 registry has no pattern 'mereological'
  * v2 → `pass`: stored 2 fact(s): mereological.part_of(session_ids=[], affirmed=1); spatial_temporal.lives_in(session_ids=[], affirmed=1)

### `p2-routing-anomaly-preference` — routing_memo → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'routing_memo'
  * v2 → `pass`: validator anomaly: validator anomaly: invariant 'user_subject_required' failed on slot 'agent' (expected "user (one of {'user', 'me', 'i'})", got 'Donald Trump')

### `p2-memo-write-mereological` — routing_memo → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'routing_memo'
  * v2 → `pass`: memo row written for ('mereological', 'part_of') → 'retrieval'

### `p2-memo-hit-mereological` — routing_memo → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'routing_memo'
  * v2 → `pass`: memo hit for ('mereological', 'part_of') → 'retrieval'

### `p3-cheetahs-storage` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): preference.dislikes/user_stored
  * v2 → `pass`: stored 1 fact(s): preference.dislikes(session_ids=[], affirmed=1)

### `p3-cheetahs-assertion` — assistant_lookup [predicate_equivalence] → **expected_divergence**

  * v1 → `fail`: v1 outcome='unverified' → lookup='miss', expected 'match'
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p3-active-passive` — two_text_oracle → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'two_text_oracle'
  * v2 → `pass`: two-text classification stored: 'equivalent', slot_reversal='subject_object_swap'

### `p3-distinct-negative` — two_text_oracle → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'two_text_oracle'
  * v2 → `pass`: two-text classification stored: 'distinct', slot_reversal='none'

### `p4-alias-resolution-positive-storage` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): spatial_temporal.lives_in/user_stored
  * v2 → `pass`: stored 1 fact(s): spatial_temporal.lives_in(session_ids=[], affirmed=1)

### `p4-alias-resolution-positive-assertion` — assistant_lookup [entity_equivalence] → **expected_divergence**

  * v1 → `fail`: v1 outcome='unverified' → lookup='miss', expected 'match'
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p4-case-disambiguation-negative-storage` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): preference.likes/user_stored
  * v2 → `pass`: stored 1 fact(s): preference.likes(session_ids=[], affirmed=1)

### `p4-case-disambiguation-negative-assertion` — assistant_lookup [entity_equivalence] → **expected_divergence**

  * v1 → `fail`: v1 outcome='verified' → lookup='match', expected 'miss'
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p4-over-merge-negative-storage` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): spatial_temporal.lives_in/user_stored
  * v2 → `pass`: stored 1 fact(s): spatial_temporal.lives_in(session_ids=[], affirmed=1)

### `p4-over-merge-negative-assertion` — assistant_lookup → **both_pass**

  * v1 → `pass`: v1 store_lookup matched expected outcome on 1 fact(s)
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p5-tax-isa-clear` — substrate_direct → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'substrate_direct'
  * v2 → `pass`: oracle 'entity_taxonomy' row stored and retrieved with label='child_subsumed_by_parent'

### `p5-tax-partof-clear` — substrate_direct → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'substrate_direct'
  * v2 → `pass`: oracle 'entity_taxonomy' row stored and retrieved with label='child_subsumed_by_parent'

### `p5-dist-likes-isa` — substrate_direct → **v2_only_by_design**

  * v1 → `not_applicable`: v1 has no equivalent code path for shape 'substrate_direct'
  * v2 → `pass`: oracle 'predicate_distribution' row stored and retrieved with label='distributes_down'

### `p6-session-local-storage` — user_storage [session_local] → **expected_divergence**

  * v1 → `fail`: v1 has no is_session_local / session_ids columns; session-aware expectations cannot be satisfied (stored: spatial_temporal.lives_in/user_stored)
  * v2 → `pass`: stored 1 fact(s): spatial_temporal.lives_in(session_ids=['A'], affirmed=1)

### `p6-session-local-in-session-match` — assistant_lookup [session_local] → **both_pass**

  * v1 → `pass`: v1 store_lookup matched expected outcome on 1 fact(s)
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p6-session-local-cross-session-miss` — assistant_lookup [session_local] → **expected_divergence**

  * v1 → `fail`: v1 outcome='verified' → lookup='match', expected 'miss'
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p7-williamstown-deriv-storage` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): spatial_temporal.lives_in/user_stored
  * v2 → `pass`: stored 1 fact(s): spatial_temporal.lives_in(session_ids=[], affirmed=1)

### `p7-williamstown-deriv-assertion` — assistant_lookup [derivation] → **expected_divergence**

  * v1 → `fail`: v1 outcome='unverified' → lookup='miss', expected 'match'
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p7-cheetahs-deriv-storage` — user_storage → **both_pass**

  * v1 → `pass`: v1 stored 1 fact(s): preference.dislikes/user_stored
  * v2 → `pass`: stored 1 fact(s): preference.dislikes(session_ids=[], affirmed=1)

### `p7-cheetahs-deriv-assertion` — assistant_lookup [derivation] → **both_fail**

  * v1 → `fail`: v1 outcome='unverified' → lookup='miss', expected 'match'
  * v2 → `fail`: served_from_tier='u', expected 'derivation'

### `p7-derivation-miss-no-substrate` — assistant_lookup → **both_pass**

  * v1 → `pass`: v1 store_lookup matched expected outcome on 1 fact(s)
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)

### `p7-derivation-miss-weighs-no-distribution` — assistant_lookup → **both_pass**

  * v1 → `pass`: v1 store_lookup matched expected outcome on 1 fact(s)
  * v2 → `pass`: walker matched expected outcome on 1 fact(s)


## Latency comparison (criterion 4)

Per-claim walker (v2) vs Router.route (v1) **best-of-30** latency (warm-cache, after 3 warmup iterations). min-of-N is the right statistic for warm-cache benchmarks: the work the code must do has a hard floor; samples can only go higher than that floor (GC, OS scheduling, etc.), never lower. Median-over-10 was noisy at sub-millisecond scale.

**Per-mode gate:** `cheap_path` ≤ 4.0× (literal / oracle Tier U / routing-anomaly); `walk_path` ≤ 15.0× (derivation BFS / fresh-tier traversal).

Two regimes because v2's algorithmic cost is path-dependent. Cheap paths pay Layer 2's two-step (validator + memo + classifier) plus walker_decision event emission. Walk paths additionally traverse multiple oracles with per-oracle event emissions across the chain. Both regimes pay Layer 5's intervention planner cost (decision_confidence computation + matrix lookup). The three cost sources are the architectural commitments these layers make: principle 7's validate-before-classifying (Layer 2), principle 4's bounded inference over substrate (walker), and principle 6's auditability through structured events (per-event SQLite writes).

On-disk numbers are informational — the on-disk/in-memory delta is per-event SQLite fsync overhead from principle 6, not algorithmic cost.

| id | mode | description | in-mem v1 (ms) | in-mem v2 (ms) | in-mem ratio | ceiling | gate | on-disk v1 (ms) | on-disk v2 (ms) | on-disk ratio |
| --- | --- | --- | ---:| ---:| ---:| ---:| --- | ---:| ---:| ---:|
| `L1_literal_tier_u` | `cheap_path` | literal Tier U match (no oracles) | 0.096 | 0.187 | 1.95× | 4.0× | ✓ | 18.921 | 54.948 | 2.90× |
| `L2_oracle_tier_u` | `cheap_path` | Tier U match via predicate_equivalence (cheetahs case) | 0.116 | 0.159 | 1.37× | 4.0× | ✓ | 20.909 | 44.810 | 2.14× |
| `L3_alias_tier_u` | `cheap_path` | Tier U match via entity_equivalence (NYC alias) | 0.129 | 0.283 | 2.20× | 4.0× | ✓ | 20.538 | 45.935 | 2.24× |
| `L4_derivation` | `walk_path` | derivation walk (Williamstown → Massachusetts) | 0.109 | 0.371 | 3.40× | 15.0× | ✓ | 23.917 | 91.308 | 3.82× |
| `L5_fresh_miss` | `walk_path` | fresh-tier miss (no substrate, no fact) | 0.113 | 0.271 | 2.39× | 15.0× | ✓ | 20.670 | 90.690 | 4.39× |
| `L6_routing_anomaly` | `cheap_path` | routing_anomaly short-circuit (preference + non-user agent) | 0.042 | 0.049 | 1.17× | 4.0× | ✓ | 21.986 | 32.302 | 1.47× |

